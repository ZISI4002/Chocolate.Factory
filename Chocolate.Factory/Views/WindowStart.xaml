<Window x:Class="Chocolate.Factory.Views.WindowStart"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chocolate.Factory.Views"
        mc:Ignorable="d"
        Title="Check connection" Height="140" Width="320" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Loaded="WindowLoaded">
    <Grid Background="WhiteSmoke">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="36*"/>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="234*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Name="FirstPanel" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="2" Margin="1,0,0,0" Grid.ColumnSpan="2" >
            <TextBlock FontWeight="Black" Text="Connecting to the server" HorizontalAlignment="Center" FontSize="17"/>
            <ProgressBar Name="ProgressBar" IsIndeterminate="True"  Height="20" Margin="10,5,10,10" Background="Transparent"/>
        </StackPanel>
        <StackPanel Name="SecondPanel" VerticalAlignment="Center" HorizontalAlignment="Left" Visibility="Hidden" Grid.ColumnSpan="3" Margin="3,0,0,0" Grid.Column="1">
            <TextBlock FontWeight="Black" Text="Serverə qoşulma zamanı xəta!!!" HorizontalAlignment="Center" FontSize="17"/>
            <Button Margin="15" Content="Server Configuration" FontWeight="DemiBold" Name="BtnConfig" Cursor="Hand" VerticalAlignment="Bottom" HorizontalAlignment="Center" >
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <TextBlock TextDecorations="Underline">
                            <ContentPresenter />
                        </TextBlock>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Foreground" Value="Blue" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Foreground" Value="Red" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

        </StackPanel>

    </Grid>
</Window>